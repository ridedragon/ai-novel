# AI小说创作助手 - 功能完善与问题修复报告

## 一、已修复的Bug日志

### 修复记录总览

| 序号 | Bug ID | 严重程度 | 修复日期 | 状态 |
|------|--------|---------|---------|------|
| 1 | 1.1 | 🔴 高危 | 2026-03-26 | ✅ 已修复 |
| 2 | 1.2 | 🟠 中高 | 2026-03-26 | ✅ 已修复 |
| 3 | 1.3 | 🔴 高危 | 2026-03-26 | ✅ 已修复 |
| 4 | 1.4 | 🟠 中高 | 2026-03-26 | ✅ 已修复 |

---

### Bug 1.1: 章节编辑器自动保存风险

**文件**: [ChapterEditor.tsx](file:///d:\Downloads\ai小说\src\components\Editor\ChapterEditor.tsx)

**问题描述**:
- 使用500ms防抖保存章节内容
- 如果用户在保存完成前关闭页面或刷新，可能丢失数据
- 没有保存状态提示

**修复内容**:
1. 添加 `beforeunload` 事件监听，在用户关闭页面时提示未保存内容
2. 添加 `isDirtyRef` 和 `hasUnsavedChanges` 状态追踪
3. 添加 `lastSavedTime` 状态显示最后保存时间
4. 在标题下方添加"保存中..."和"已保存"状态提示

**修复代码**:
```typescript
// 添加 beforeunload 事件
useEffect(() => {
  const handleBeforeUnload = (e: BeforeUnloadEvent) => {
    if (isDirtyRef.current) {
      e.preventDefault();
      e.returnValue = '您有未保存的更改，确定要离开吗？';
      return e.returnValue;
    }
  };
  window.addEventListener('beforeunload', handleBeforeUnload);
  return () => window.removeEventListener('beforeunload', handleBeforeUnload);
}, []);

// 保存状态UI
{hasUnsavedChanges && (
  <span className="text-[9px] text-amber-500 font-medium animate-pulse">保存中...</span>
)}
{!hasUnsavedChanges && lastSavedTime && (
  <span className="text-[9px] text-green-500/60 font-medium" title={`最后保存: ${lastSavedTime.toLocaleTimeString()}`}>
    已保存
  </span>
)}
```

---

### Bug 1.2: 工作流节点删除逻辑异常

**文件**: [WorkflowEditor.tsx](file:///d:\Downloads\ai小说\src\components\WorkflowEditor.tsx)

**问题描述**:
- 点击节点直接打开属性面板
- 没有双击或右键菜单来区分选择和编辑操作
- 可能会导致误操作

**修复内容**:
1. 添加 `selectedNodeId` 状态追踪当前选中的节点
2. 添加 `lastClickTimeRef` 检测双击
3. 实现双击打开属性面板，单击仅选中节点
4. 添加右键菜单功能，支持"编辑节点"和"删除节点"
5. 添加 `onPaneClick` 清除选中状态

**修复代码**:
```typescript
// 双击检测逻辑
const onNodeClick = useCallback((_: any, node: Node) => {
  const now = Date.now();
  const timeSinceLastClick = now - lastClickTimeRef.current;

  if (timeSinceLastClick < 300 && selectedNodeId === node.id) {
    setEditingNodeId(node.id);  // 双击：打开编辑
  } else {
    setSelectedNodeId(node.id); // 单击：仅选中
  }
  lastClickTimeRef.current = now;
}, [selectedNodeId]);

// 右键菜单
const onNodeContextMenu = useCallback((event: React.MouseEvent, node: Node) => {
  event.preventDefault();
  setSelectedNodeId(node.id);
  setEditingNodeId(node.id);
  setContextMenu({ nodeId: node.id, x: event.clientX, y: event.clientY });
}, []);
```

---

### Bug 1.3: 正则表达式执行风险

**文件**:
- [aiHelpers.ts](file:///d:\Downloads\ai小说\src\utils\aiHelpers.ts)
- [RegexEditorModal.tsx](file:///d:\Downloads\ai小说\src\components\Modals\RegexEditorModal.tsx)

**问题描述**:
- 正则脚本由用户自定义编写
- 恶意或有缺陷的正则可能导致ReDoS攻击
- 浏览器卡死或无限循环

**修复内容**:
1. 新增 `validateRegexSafety()` 函数，检测危险正则模式
2. 新增 `safeRegexReplace()` 函数，实现带超时保护的替换
3. 在 `applyRegexToText()` 中添加总超时限制(5秒)和单脚本超时(500ms)
4. 在正则编辑器中实时验证正则安全性，阻止不安全正则保存

**修复代码**:
```typescript
// 危险模式检测
const dangerousPatterns = [
  /\(\?[^)]*\+\[^)]*\)\*/i,  // (?:x+)* 类型的嵌套量词
  /\(\[.*?\]\+\)\*/i,          // ([abc]+)* 类型
  /\.\*\.\*/i,                  // 连续.*容易导致回溯
  /\([^)]*\)\{[^}]*\}\{/i,     // 嵌套量词
];

// 安全替换
export const safeRegexReplace = (
  text: string,
  regex: RegExp,
  replaceStr: string,
  timeoutMs: number = 1000
): { result: string; timedOut: boolean } => {
  const startTime = Date.now();
  let result = text;
  let timedOut = false;

  result = text.replace(regex, (...args) => {
    if (Date.now() - startTime > timeoutMs) {
      timedOut = true;
      return args[0];
    }
    return replaceStr;
  });

  if (timedOut) {
    terminal.warn(`[REGEX] 正则执行超时(${timeoutMs}ms)，跳过该脚本`);
    return { result: text, timedOut: true };
  }

  return { result, timedOut };
};
```

---

### Bug 1.4: 文件上传安全风险

**文件**: [ReferenceManager.tsx](file:///d:\Downloads\ai小说\src\components\ReferenceManager.tsx)

**问题描述**:
- 允许上传任意文件类型
- 没有文件大小硬限制
- 没有恶意文件扫描

**修复内容**:
1. 添加单个文件大小限制 (10MB)
2. 添加允许的文件类型白名单
3. 添加文件扩展名验证
4. 添加总文件大小警告 (50MB)
5. 显示详细的错误信息

**修复代码**:
```typescript
const MAX_FILE_SIZE = 10 * 1024 * 1024; // 10MB per file
const ALLOWED_TYPES = [
  'text/plain', 'text/markdown', 'text/html',
  'application/pdf',
  'image/jpeg', 'image/png', 'image/gif', 'image/webp',
  'application/vnd.openxmlformats-officedocument.wordprocessingml.document'
];
const ALLOWED_EXTENSIONS = ['.txt', '.md', '.html', '.pdf', '.jpg', '.jpeg', '.png', '.gif', '.webp', '.docx'];

// 验证所有文件
for (let i = 0; i < files.length; i++) {
  const file = files[i];
  if (file.size > MAX_FILE_SIZE) {
    oversizedFiles.push(`${file.name} (${(file.size / 1024 / 1024).toFixed(2)}MB)`);
    continue;
  }
  const hasValidType = ALLOWED_TYPES.includes(file.type);
  const hasValidExtension = ALLOWED_EXTENSIONS.some(ext => file.name.toLowerCase().endsWith(ext));
  if (!hasValidType && !hasValidExtension) {
    invalidFiles.push(file.name);
  }
}
```

---

## 二、功能缺失与混乱问题分析

### 2.1 UI/UX问题

#### 问题 2.1.1: 版本切换命名混乱
**位置**: ChapterEditor.tsx

**问题**: 上一版本/下一版本的命名容易混淆
```typescript
// currentVersionIndex 增加表示"上一版本"（数字上更小）
const goToPreviousVersion = () => {
  if (currentVersionIndex < totalVersions - 1) {
    setCurrentVersionIndex(currentVersionIndex + 1);
  }
};
```

**建议**: 改用"最早版本"/"最新版本"或"回退"/"前进"的命名

#### 问题 2.1.2: 按钮缺少loading状态
**位置**: 多个组件

**问题**: 异步操作（保存、删除、生成）的按钮没有显示loading状态

**建议**: 添加 Loader2 图标显示加载状态

---

### 2.2 输入验证问题

#### 问题 2.2.1: 创建小说名称验证
**位置**: CreateNovelModal.tsx

**问题**: 虽然按钮有 disabled 属性，但没有必填提示

**建议**: 添加红色星号和"名称不能为空"提示

#### 问题 2.2.2: 章节标题验证
**位置**: OutlineManager.tsx

**问题**: 可以创建空标题的章节

**建议**: 添加前端验证，标题至少1个字符

---

### 2.3 错误处理问题

#### 问题 2.3.1: 静默catch块过多
**位置**: 全局

**问题**: 53个catch块，其中很多只是空块或简单console.error

**建议**:
- 添加terminal日志记录
- 向用户显示友好错误提示
- 某些关键错误应该中断流程而不是静默继续

#### 问题 2.3.2: alert() 使用不统一
**位置**: 多个文件

**问题**: 使用原生alert()而不是自定义提示组件

**建议**: 考虑统一的Toast提示组件

---

### 2.4 状态管理问题

#### 问题 2.4.1: App.tsx 状态过重
**位置**: App.tsx

**问题**: 大量状态在顶层管理，任何状态变化可能导致级联重渲染

**建议**:
- 使用 React Context 分离状态
- 或考虑引入 Zustand/Redux 等状态管理库

#### 问题 2.4.2: ref和state混用
**位置**: useAutoWriteManager.ts 等

**问题**: 某些地方ref和state混用可能导致逻辑混乱

**示例**:
```typescript
useEffect(() => {
  if (autoWriteStatusRef.current) {
    setAutoWriteStatus(autoWriteStatusRef.current);
  }
}, [isAutoWriting]);
```

---

### 2.5 TypeScript类型安全

#### 问题 2.5.1: 大量使用 `as any`
**位置**: 全局

**问题**: 绕过TypeScript类型检查，增加运行时错误风险

**示例**:
```typescript
handleUpdate({ _deleted: true } as any);
(updates as any)._deleted
```

**建议**: 定义专用的联合类型或枚举来替代 `as any`

---

### 2.6 性能问题

#### 问题 2.6.1: 大列表无虚拟滚动
**位置**: 多个Manager组件

**问题**: 章节/角色/灵感列表没有虚拟滚动，数据量大时可能卡顿

**建议**: 使用 react-window 或 react-virtualized 实现虚拟列表

#### 问题 2.6.2: 防抖时间不统一
**位置**: 多个组件

**问题**: 保存防抖500ms，其他操作时间不统一

**建议**: 抽取为统一配置常量

---

## 三、待处理问题清单

### 高优先级
| 序号 | 问题 | 建议方案 |
|------|------|---------|
| 1 | 版本切换命名混乱 | 改进UI命名 |
| 2 | 状态管理过重 | 使用Context分离或引入状态管理库 |

### 中优先级
| 序号 | 问题 | 建议方案 |
|------|------|---------|
| 1 | 按钮缺少loading状态 | 统一添加加载状态 |
| 2 | 输入验证缺失 | 添加前端验证和提示 |
| 3 | 静默catch块过多 | 改进错误日志 |
| 4 | TypeScript类型安全 | 移除 as any |
| 5 | ref和state混用 | 重构状态管理逻辑 |

### 低优先级
| 序号 | 问题 | 建议方案 |
|------|------|---------|
| 1 | alert() 使用不统一 | 引入Toast组件 |
| 2 | 大列表无虚拟滚动 | 实现虚拟列表 |
| 3 | 防抖时间不统一 | 抽取为配置常量 |

---

## 四、安全建议

### 4.1 API密钥安全
- 当前存储在LocalStorage，存在XSS攻击风险
- 建议增加加密或使用httpOnly cookie

### 4.2 用户输入转义
- 某些用户输入直接渲染到HTML
- 建议使用DOMPurify等库进行XSS防护

### 4.3 CORS配置依赖
- 没有验证用户配置的Base URL是否支持CORS
- 建议在设置时进行连接测试

---

## 五、测试建议

### 5.1 单元测试
- 状态更新逻辑测试
- 防抖/节流函数测试
- 正则表达式执行测试
- 文件上传验证测试

### 5.2 集成测试
- 工作流执行流程测试
- 章节保存和加载测试
- 多模块数据关联测试

### 5.3 端到端测试
- 完整创作流程测试
- 边界条件测试（空数据、大数据）

---

*报告生成时间: 2026-03-26*
